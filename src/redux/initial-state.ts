/**
 * Initial state of the app
 *
 * `isLoggedIn`: State to know whether the user is logged in or not
 *
 * `firstName`: first name of the user
 *
 * `lastName`: last name of the user
 */
export const initialState = {
  isLoggedIn: false,
  firstName: "",
  lastName: "",
};
