/**
 * Formatted type for the cookie
 */
export type cookieType = {
  name: string;
  value: string;
};

/**
 * Formatted type for the array of cookies
 */
export type arrayOfCookiesType = {
  name: string;
  value: string;
}[];
